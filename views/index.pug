extends master
block content
    form(action="/" method="POST" id="agregar-url")
        legend Añade tu URL en el siguiente formulario
        .campo
            input(type="url" id="urlOriginal" name="urlOriginal" placeholder="Añade tu URL" required)

        .campo.enviar
            input.btn(type="submit" value="acortar")
        
